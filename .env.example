APP_ENV=development
UPLOAD_DIR=uploads
OUTPUT_DIR=outputs
SOUND_FONT_PATH=assets/piano.sf2

# Experimental: after preprocess, write {task_id}.stem.vocal.wav + .stem.accompaniment.wav
# under OUTPUT_DIR and transcribe the vocal stem only (stub seam until a real separator is wired).
# H2S_EXPERIMENTAL_TWO_STEM_SEPARATION=true

# Gateway-first LLM (placeholders only; do not put provider keys in browser)
# For gateway/proxy or future backend; frontend does not auto-load repo .env.
LLM_BASE_URL=
LLM_MODEL=
LLM_AUTH_TOKEN=
